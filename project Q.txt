Q1:select *from (SELECT f.title AS film_title, c.name AS category_name, count(r.rental_id) AS rental_countFROM film_category fcJOIN category cON c.category_id = fc.category_idJOIN film fON f.film_id = fc.film_idJOIN inventory iON i.film_id = f.film_idJOIN rental rON r.inventory_id = i.inventory_idWHERE c.name IN ( 'Animation', 'Children', 'Classics', 'Comedy', 'Family', 'Music')GROUP BY 1, 2ORDER BY 2, 1) T1_____________________________Q2:WITH t1 AS (SELECT f.title title, c.name AS name, f.rental_duration rental_duration, NTILE(4) OVER (ORDER BY f.rental_duration)  standard_quartileFROM category cJOIN film_category fcON c.category_id = fc.category_idJOIN film fON f.film_id = fc.film_idWHERE c.name IN ('Animation', 'Children', 'Classics', 'Comedy', 'Family', 'Music') ) SELECT title, name, rental_duration, SUM(standard_quartile) standard_quartileFROM t1GROUP BY 1,2,3ORDER BY 3,4 DESC__________________________________Q3:WITH t1 AS (SELECT f.title title, c.name AS name, f.rental_duration rental_duration, NTILE(4) OVER (ORDER BY f.rental_duration) standard_quartileFROM category cJOIN film_category fcON c.category_id = fc.category_idJOIN film fON f.film_id = fc.film_idWHERE c.name IN ('Animation', 'Children', 'Classics', 'Comedy', 'Family', 'Music')) SELECT name, standard_quartile, COUNT(standard_quartile) countFROM t1GROUP BY 1,2 ORDER BY 1,2________________________________Q4:SELECT DATE_PART('month', r.rental_date) AS rental_month, DATE_PART('year', r.rental_date) AS rental_year, i.store_id AS store_id, COUNT(*) AS rental_countFROM rental rJOIN inventory iON i.inventory_id = r.inventory_idGROUP BY 1,2,3ORDER BY 4 DESC